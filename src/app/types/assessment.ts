/** Shared posture / scan types (used by MoveNet pipeline + optional Supabase). */

export type RiskCategory = 'low' | 'medium' | 'high';
export type PostureLevel = 'beginner' | 'intermediate' | 'advanced';
export type SeverityBand = 'mild' | 'moderate' | 'severe';
export type IntendedView = 'front' | 'side' | 'back';
